plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.vedika.heath.vedika_healthcare"  // ✅ Correct

    compileSdk = flutter.compileSdkVersion
    ndkVersion "29.0.13113456"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true  // ✅ REQUIRED

    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.vedika.heath.vedika_healthcare"
        minSdk = 26
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    signingConfigs {
        release {
            storeFile file("C:\\Users\\DELL\\AndroidStudioProjects\\vedika_app\\my-release-key.jks")
            storePassword "Krushna@01" // Enter your keystore password here
            keyAlias "my-key-alias" // Enter your key alias here
            keyPassword "Krushna@01" // Enter your key password here
        }
    }

    buildTypes {
        release {
            // Enable code shrinking and obfuscation (optional)
            minifyEnabled true  // Enables minification for release
            shrinkResources true // Shrinks unused resources (optional)

            // Use Proguard rules (if needed)
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release // Apply the release signing config
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation 'com.google.android.gms:play-services-maps:19.0.0'  // ✅ Google Maps SDK
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.5'
    implementation 'androidx.multidex:multidex:2.0.1'
    implementation 'phonepe.intentsdk.android.release:IntentSDK:5.0.1'
}